% Entry point
solve_4_queens(Solution) :-
    Solution = [Q1, Q2, Q3, Q4],
    permutation([1,2,3,4], Solution),
    safe(Solution).

% Check that no two queens attack each other diagonally
safe([]).
safe([Queen|Others]) :-
    safe(Others),
    no_attack(Queen, Others, 1).

% Ensure no diagonal attacks
no_attack(_, [], _).
no_attack(Q, [Q1|Qs], D) :-
    Q =\= Q1 + D,
    Q =\= Q1 - D,
    D1 is D + 1,
    no_attack(Q, Qs, D1).
```

Query: ?- solve_4_queens(Solution).
Output:

Solution = [2, 4, 1, 3] ;
Solution = [3, 1, 4, 2] ;
false.
These are the two valid solutions to the 4-Queens problem.
